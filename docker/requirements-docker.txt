# Minimal requirements for Docker builds
# PyTorch is installed separately for CPU/GPU optimization

# Core dependencies
numpy>=1.21.0
scikit-learn>=1.0.0

# Essential audio/ML libraries
speechbrain>=0.5.0
pyannote.audio>=3.1.0
openai-whisper>=20230314

# Audio processing (essential only)
librosa>=0.9.0
soundfile>=0.12.0

# Configuration
python-dotenv>=0.19.0

# Hugging Face (essential)
transformers>=4.20.0
accelerate>=0.20.0