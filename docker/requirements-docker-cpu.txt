# Docker-specific requirements for Speaker-Profiles (CPU version)
# Core ML/AI Libraries (PyTorch installed separately in Docker)
numpy>=1.21.0,<1.25.0
scikit-learn>=1.0.0,<1.4.0

# Speaker Recognition and Audio Processing
speechbrain>=0.5.0,<1.0.0
pyannote.audio>=3.1.0,<4.0.0
openai-whisper>=20230314

# Audio Processing
librosa>=0.9.0,<1.0.0
soundfile>=0.12.0

# Environment and Configuration
python-dotenv>=0.19.0

# For Hugging Face models
transformers>=4.20.0,<5.0.0
accelerate>=0.20.0

# Additional dependencies for Docker environment
datasets>=2.0.0
tokenizers>=0.13.0
safetensors>=0.3.0

# For better audio handling in containers
pydub>=0.25.0

# For potential web interface or API
fastapi>=0.68.0
uvicorn>=0.15.0

# For model optimization (CPU version)
onnx>=1.12.0
onnxruntime>=1.12.0

# Utilities
tqdm>=4.64.0
click>=8.0.0
rich>=12.0.0